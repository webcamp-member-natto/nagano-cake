<h4>注文情報確認</h4>

<div class="container">
  <div class="row">
    <div class="col">
<table class="table">
  <thead>
    <tr>
      <th scope="col">商品名</th>
      <th scope="col">単価(税込)</th>
      <th scope="col">数量</th>
      <th scope="col">小計</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% @cart_item.each do |cart_item| %>
    <tr>
      <td>
        <%= image_tag cart_item.item.image %></br>
        <%= cart_item.item.name %>
      <td><%= cart_item.item.quantity %></td>
      <td><%= %></td>
      <td></td>
      <td></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "買い物を続ける", public_items_path %>

