<div class="container">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
    <span style="background-color:lightgrey; font-size:30px;">配送先編集</span>
          <%= form_with model: @shipping_address, url: public_shipping_address_path, method: :patch do |f| %>
              <table>
                 <tr>
                    <div class="field">
                      <th><%= f.label "郵便番号(ハイフンなし)" %></th>
                      <td><%= f.text_field :postal_code,placeholder: "0000000" %></td>
                    </div>
                 </tr>
                 <tr>
                    <div class="field">
                      <th><%= f.label "住所" %></th>
                      <td><%= f.text_field :address,placeholder: "東京都渋谷区代々木神園町0-0" %></td>
                    </div>
                 </tr>
                 <tr>
                    <div class="field">
                      <th><%= f.label "宛名" %></th>
                      <td><%= f.text_field :name,placeholder: "令和道子"%></td>
                    </div>
                 </tr>
               </table>
               <%= f.submit "変更内容を保存", class: 'btn btn-success' %>
          <% end %>
</div>